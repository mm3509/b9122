#include <math.h>
#include <stdbool.h>
#include <stdio.h>

int print_primes(int n) {
  // TODO: complete this function to print the prime numbers from 2
  // until n, in ascending order, and each on a separate
  // line. Autograder will only use this function.
 
  return 0;
}

int main() {
  // Note: this serves to debug. Autograder will ignore this
  // function. When you run this file, you should see the example
  // output in the exam.
  
  int n = 11;
  printf("List of primes until %d:\n", n);
  print_primes(n);
  return 0;
}
